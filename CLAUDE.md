# CLAUDE.md

このリポジトリで作業するときの前提と規約。**現在の開発対象は `web/` 配下の
Web版**。Python版（`app.py`, `src/`, `tests/`）は `v0.1-python` タグで凍結済み。

## このプロジェクトは何か

**Brushlink**。TIBCO Spotfire に着想を得た、**クロスフィルタ（マーキング）**
を再実装した探索的データ分析ダッシュボード。就職活動用のポートフォリオ。

**機能を増やすことより、クロスフィルタの完成度を上げることを優先する。**
チャートの種類を増やす提案は、連動が完成するまで保留すること。

## 製品方針

- 対象は 2 層。データサイエンティストがノーコードで使えること、
  初心者が説明なしに触れること
- 両者は完全には両立しないため、初期状態は自動で組み立て、
  操作すれば細かく変更できる設計にする
- ターミナル操作を前提にした機能は作らない。CLI 引数・コマンドは開発用の補助に留める

## Python版について（凍結）

`app.py` / `src/` / `tests/` は Panel + HoloViews + DuckDB(pandas) による
初期実装。`git tag v0.1-python` として凍結し、以後の機能追加はしない
（バグ修正が必要になった場合も、まず Web 版への影響がないか検討する）。

**作り直した理由**: Panel 版は `panel serve` によるサーバー常駐が前提で、
使うたびに Python 環境とプロセスが必要だった。DuckDB-WASM でブラウザ内に
完結させれば、静的ファイルを配るだけで動き、研究データもサーバーに一切
送らずに閉じられる（製品方針のターミナル非依存ともより強く整合する）。
`web/` での技術検証（`docs/performance.md`）で実用の目処が立ったため、
以後の開発は Web 版を主とする。

Python版時代の設計判断（Panel を選んだ理由、フィルタと選択の分離など）は
`git show v0.1-python:README.md` で参照できる。

## 起動と検証

```bash
cd web
npm install
npm run dev              # 開発サーバ（http://localhost:5173/brushlink/）
npm run build             # 本番ビルド（tsc の型検査を含む）
npm run preview           # ビルド結果をローカルで確認
```

自動テストはまだ無い。変更後は最低限 `npm run build` の型検査を通し、
可能であればブラウザで実際に操作して確認する
（クロスフィルタは目視確認が本質的に必要な機能のため、将来的にも
Playwright 等のブラウザ駆動テストを検討する。現状は都度スクリプトを
一時的に用意して検証し、確認後に削除している）。

デプロイは `gh-pages` ブランチに `web/dist` の中身を置く方式。
公開URL: https://soma2028.github.io/brushlink/

## 技術構成（Web版）

| 層 | 実装 | 役割 |
|---|---|---|
| データ | DuckDB-WASM | ブラウザ内で動くインメモリ列指向エンジン。CSV/Excelを読み込みSQLで扱う |
| 可視化・連動 | Mosaic (`@uwdata/vgplot`) | チャート間の選択連動（`Selection.crossfilter`）とSQL集計の橋渡し |
| 言語 | TypeScript | 型検査を通す。`any` は最小限に |
| ビルド | Vite | 開発サーバ・本番ビルド |
| 配信 | GitHub Pages | `gh-pages` ブランチから配信。`vite.config.ts` の `base` を `/brushlink/` に固定 |

```
web/
  index.html
  vite.config.ts     base: '/brushlink/' を固定
  src/
    main.ts          現状すべてのロジックがここに入っている（技術検証の名残）
    style.css
  public/
    favicon.svg
docs/
  performance.md      データ量とパフォーマンスの計測結果・根拠
```

`src/main.ts` は現状 1 ファイルに全部入っている。「次にやること」の
各項目を実装する際は、Python版の `app.py` → `src/*` の分離
（UI組み立てとロジックを分ける）を意識してモジュールを切り出していくこと。
ただし今は無理に先取りしてモジュール構成を作らない
（使われない抽象化を残さないという方針に沿う）。

## 決まっていること

- **散布図は行数 50,000 件を境に、dot マークと raster マークを自動で
  切り替える。** 50,000 件未満は dot、以上は raster。
- **切り替えをユーザーに選ばせる UI は作らない。** 行数から自動判定する
  だけにする（製品方針の「初期状態は自動で組み立てる」に沿う）。
- **根拠**: `docs/performance.md` の計測。dot は 10 万行でドラッグ応答
  518ms、100万行で6,822ms と行数に対して非線形に悪化する一方、
  raster は 1万〜100万行を通じて157〜158msでほぼ一定。10万行の時点で
  体感できる遅延（500ms超）が生じ始めるため、そこから十分小さい側に
  倒して 50,000 を閾値とした。**ただし 1万〜10万の間そのものは計測して
  おらず（`docs/performance.md` の「まだ分かっていないこと」参照）、
  50,000 という数値自体は計測で直接確認した境界ではなく、既知の安全域
  （1万）と既知の危険域（10万）の間で安全側に寄せた暫定値**である。
  より細かい計測ができたら見直す。

## コード規約

- **コメントと UI 文言は日本語**。変数名・関数名は英語。
- TypeScript の型を明示する。`tsconfig.json` の strict を維持し、
  `any` は既存コードにあるもの（Mosaic 側の型定義が薄い箇所）以外では使わない。
- 関数には何をするかだけでなく、**なぜそうしたか**を書く。設計判断が読み取れることを優先する。
- 外部ライブラリを増やすときは、既存の依存で代替できないか先に検討する。
  依存を足す場合は `package.json` と README の構成表を同時に更新する。

## 環境上の注意

- DuckDB-WASM 本体は初回アクセス時に jsDelivr から取得される。
  オフライン環境やCDNブロック環境では動かない。
- `docs/performance.md` の計測はヘッドレス Chromium（Playwright経由）で
  行った。Safari / Firefox での動作・性能は未検証。
- GitHub Pages 配信は `base: '/brushlink/'` 固定。ローカル開発時も
  `npm run dev` の URL が `/brushlink/` 始まりになる。

## 次にやること

優先度順。上から着手する。

1. ~~**ファイル読み込み（CSV / Excel）**~~ — 完了
   （`web/src/upload.ts`。ドロップ領域 + プレビュー表の行クリックで
   ヘッダ行を選ぶ方式。自動推定を初期選択にし、外れても選び直せる。
   SheetJSで両形式を共通処理し、DuckDB-WASMにテーブル登録。
   チャート・統計との連携は未実装 — 登録されたテーブルは
   'uploaded' という別名で、既存の散布図（'points'）とは独立している）

2. **軸選択とフィルタパネル**
   X/Y軸・色分けのセレクタと、母集団を絞るフィルタ（数値はレンジ、
   カテゴリはチェックボックス）。Python版と同様、フィルタとチャートの
   選択は別系統とし、フィルタで母集団を絞った「中で」選択する。

3. **統計量と選択件数の表示**
   選択中/母集団/全体の件数、数値列の要約統計量。欠測件数も
   Python版と同様に見える形にする（測定漏れがあるデータが前提のため）。

4. **単回帰・群間比較**
   `simple-statistics` または `jStat` を使う（現時点でどちらを使うか
   未確定。単回帰・相関係数が主目的なら `simple-statistics` の方が
   軽量な見込みだが、検定量が要る群間比較まで見据えると `jStat` が
   必要になる可能性がある。実装時に必要な関数の網羅性で判断する）。

5. **図の書き出し**
   選択範囲のチャートを画像として書き出す。CSV書き出しではなく図の
   書き出しである点に注意（Python版の「次にやること」とは異なる）。

6. **k-means / PCA / 変数重要度**
   選択範囲に対する簡易機械学習。Python版で計画していた
   scikit-learn 版（`src/ml.py`、UMAPオプション依存）と同等の内容を
   JS実装で置き換える形。ライブラリは実装時に選定する。

## やらないこと

- **サーバーを必要とする構成に戻さないこと。** Python/Panel のような
  常時稼働サーバー前提の構成には戻らない。ブラウザ完結を維持する
  （作り直した理由そのものであるため）。
- 認証・マルチユーザー対応（ポートフォリオの範囲外）
- 深層学習ライブラリ（CNN 等）の導入。表形式データと手法が噛み合わず、
  クロスフィルタという軸から外れるため
