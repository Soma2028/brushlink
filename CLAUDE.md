# CLAUDE.md

このリポジトリで作業するときの前提と規約。**現在の開発対象は `web/` 配下の
Web版**。Python版（`app.py`, `src/`, `tests/`）は `v0.1-python` タグで凍結済み。

## このプロジェクトは何か

**Brushlink**。TIBCO Spotfire に着想を得た、**クロスフィルタ（マーキング）**
を再実装した探索的データ分析ダッシュボード。就職活動用のポートフォリオ。

**機能を増やすことより、クロスフィルタの完成度を上げることを優先する。**
チャートの種類を増やす提案は、連動が完成するまで保留すること。
（2026-09 にユーザーの指示でグラフの種類を追加した。条件は「どのグラフも
$brush / $filter に参加して連動すること。参加できないグラフは追加しない」。
以後も種類を足すときはこの条件を守る。「決まっていること」参照）

## 製品方針

- 対象は 2 層。データサイエンティストがノーコードで使えること、
  初心者が説明なしに触れること
- 両者は完全には両立しないため、初期状態は自動で組み立て、
  操作すれば細かく変更できる設計にする
- ターミナル操作を前提にした機能は作らない。CLI 引数・コマンドは開発用の補助に留める

## Python版について（凍結）

`app.py` / `src/` / `tests/` は Panel + HoloViews + DuckDB(pandas) による
初期実装。`git tag v0.1-python` として凍結し、以後の機能追加はしない
（バグ修正が必要になった場合も、まず Web 版への影響がないか検討する）。

**作り直した理由**: Panel 版は `panel serve` によるサーバー常駐が前提で、
使うたびに Python 環境とプロセスが必要だった。DuckDB-WASM でブラウザ内に
完結させれば、静的ファイルを配るだけで動き、研究データもサーバーに一切
送らずに閉じられる（製品方針のターミナル非依存ともより強く整合する）。
`web/` での技術検証（`docs/performance.md`）で実用の目処が立ったため、
以後の開発は Web 版を主とする。

Python版時代の設計判断（Panel を選んだ理由、フィルタと選択の分離など）は
`git show v0.1-python:README.md` で参照できる。

## 起動と検証

```bash
cd web
npm install
npm run dev              # 開発サーバ（http://localhost:5173/brushlink/）
npm run build             # 本番ビルド（tsc の型検査を含む）
npm run preview           # ビルド結果をローカルで確認
```

変更後は最低限 `npm run build` の型検査と、ブラウザ駆動の自動テストを通す。

```bash
npm run test:e2e         # Chromium で e2e テスト（web/e2e/、本番ビルドを自動で作って配信）
npm run test:e2e:all     # Chromium / WebKit（Safari 相当）/ Firefox の3エンジン
npx playwright install   # 初回のみ。WebKit・Firefox 本体の取得
```

- テストは `web/e2e/` に置く。クロスフィルタは「ドラッグしたら他が変わる」ことが
  本質なので、関数単位ではなく実ブラウザで本番ビルドを操作して確かめる。
- DuckDB-WASM 本体を jsDelivr から取るので、テストにもネットワークが要る。
- 不具合を直したら、再発防止のテストを足す（既存テストの名前に経緯を書いてある）。
- テストで確かめにくい見た目（レイアウト・配色）は、スクリーンショットを撮って目で見る。
  性能の計測など一時的なスクリプトは、従来どおり確認後に削除する。

デプロイは `gh-pages` ブランチに `web/dist` の中身を置く方式。
公開URL: https://soma2028.github.io/brushlink/

## 技術構成（Web版）

| 層 | 実装 | 役割 |
|---|---|---|
| データ | DuckDB-WASM | ブラウザ内で動くインメモリ列指向エンジン。CSV/Excelを読み込みSQLで扱う |
| 可視化・連動 | Mosaic (`@uwdata/vgplot`) | チャート間の選択連動（`Selection.crossfilter`）とSQL集計の橋渡し |
| 統計 | jStat | t 分布・χ² 分布の累積分布関数（p 値）のみ。集計自体は DuckDB 側 |
| 機械学習 | 自前実装（`ml.ts`、Web Worker）＋ ml-hclust | k-means・PCA・ランダムフォレストは自前、階層クラスタリングは ml-hclust の agnes |
| 統計グラフの描画 | Observable Plot（`@observablehq/plot`） | vgplot にマークの無い相関行列・Q-Q・バイオリン図の描画だけに使う。集計と連動は Mosaic |
| テスト | Playwright (`@playwright/test`) | 本番ビルドをブラウザで操作する e2e テスト（開発時のみ） |
| 言語 | TypeScript | 型検査を通す。`any` は最小限に |
| ビルド | Vite | 開発サーバ・本番ビルド |
| 配信 | GitHub Pages | `gh-pages` ブランチから配信。`vite.config.ts` の `base` を `/brushlink/` に固定 |

```
web/
  index.html
  vite.config.ts     base: '/brushlink/' を固定
  src/
    main.ts          画面の組み立てと各モジュールの配線（Selection 3本の定義もここ）
    upload.ts        CSV/Excel の読み込みと DuckDB への登録
    sample.ts        「サンプルデータで試す」用の合成データ生成
    filters.ts       絞り込みパネル（$filter、母集団の絞り込み）
    charts.ts        各グラフの組み立て（散布図・ヒストグラム・棒・誤差棒・折れ線）
    chartTypes.ts    列の型から選べるグラフの種類と、初期表示の自動決定（純粋な規則）
    chartGrid.ts     グラフのカードの並び（追加・削除・設定変更・リサイズ）
    statCharts.ts    相関行列・Q-Q・バイオリン図（Plot で描き、選択は自前で $brush に書く）
    hclustPanel.ts   階層クラスタリングのタブ（樹形図・クラスタ付きヒートマップ・列の書き戻し）
    rasterPatch.ts   vgplot の raster マークが0行の結果で落ちる不具合への対処
    settle.ts        ドラッグが止まってから追従する Selection のミラー
    stats.ts         件数・要約統計量・選択中 vs 選択外の比較
    categories.ts    カテゴリ構成の比較（χ² 検定）
    regression.ts    単回帰の集計と文章化
    inference.ts     検定（Welch の t、χ²、選択外の分散の逆算）。jStat はここだけ
    insights.ts      選択範囲の特徴の文章要約
    rows.ts          選択中の行のデータ表
    ml.ts            k-means・PCA・ランダムフォレスト（DOM に触れない純粋な計算）
    ml.worker.ts     ml.ts を別スレッドで動かす Web Worker（ml-protocol.ts が型）
    mlPanel.ts       機械学習タブの画面と、結果の列としての書き戻し
    export.ts        図の書き出し（SVG の合成と PNG 変換）
    sql.ts / dom.ts  識別子のクォート・述語の SQL 化 / エスケープ・用語の吹き出し・通知
    types/jstat.d.ts jStat の型宣言（使う関数だけ）
    style.css
  public/
    favicon.svg
  e2e/               Playwright のブラウザ駆動テスト（helpers.ts に共通手順）
  playwright.config.ts
docs/
  performance.md      データ量とパフォーマンスの計測結果・根拠
  screenshot.png      README 用の画面
```

「次にやること」の各項目を実装する際は、Python版の `app.py` → `src/*` の分離
（UI組み立てとロジックを分ける）を意識してモジュールを切り出していくこと。
ただし今は無理に先取りしてモジュール構成を作らない
（使われない抽象化を残さないという方針に沿う）。

## 決まっていること

- **散布図は行数 15,000 件を境に、dot マークと raster マークを自動で
  切り替える。** 15,000 件未満は dot、以上は raster。
- **切り替えをユーザーに選ばせる UI は作らない。** 行数から自動判定する
  だけにする（製品方針の「初期状態は自動で組み立てる」に沿う）。
- **根拠**: `docs/performance.md`「1万〜10万行の再計測（2026-09）」。
  ヒストグラムをドラッグ中に最も長く止まった1フレームは、dot では行数に
  ほぼ比例して伸びる（1万 51ms、1.5万 76ms、1.75万 80ms、2万 93〜109ms、
  5万 278ms、10万 607ms）。raster は 1万〜10万で long task が出なかった。
  入力への応答が 100ms を超えると遅れとして体感されるため、100ms を明確に
  下回った最大の計測点 15,000 を閾値にした。以前の 50,000 は 1万〜10万を
  計測しないまま置いた暫定値で、実測すると 5万行では毎フレーム約 280ms
  止まっていた（dot を母集団・選択の2層にしたことで、当初の技術検証より
  1点あたりの描画コストも増えている）。
  **計測環境はヘッドレス Chromium・同一マシンのみ**。Safari / Firefox や
  非力な端末では境界が下がりうる。

- **Selection は3本で役割を分ける**（`main.ts` 冒頭のコメント参照）。
  `$filter`（絞り込み = 母集団）、`$brush`（チャート間の crossfilter。`$filter` を含む）、
  `$selected`（`$brush` と同じ条件の intersect）。crossfilter は自分のチャートの
  選択を自分に効かせないので、チャートの外で「選択中の行そのもの」を知りたい
  もの（統計量・回帰・データ表・機械学習）は `$selected` 側を使う。
- **集計系のクライアントは `settledMirror`（`settle.ts`）経由で繋ぐ。**
  チャートと選択件数だけが即時に追従し、統計量・カテゴリ・回帰・データ表は
  ドラッグが 180ms 止まってから集計する。根拠: 全部を即時にすると 49,000 行で
  ドラッグを離してから約3.5秒遅れた（DuckDB-WASM のクエリが1本のキューで詰まる）。
  分けた後は選択件数が即時、統計量が 0.4〜0.8秒。新しい集計を足すときもこれに従う。
- **統計量として表示する値のクライアントは `filterStable: false` にする。**
  Mosaic の事前集計はブラシ範囲をピクセル単位に丸めるため、件数が厳密値から
  1件単位でずれる（選択 847 件に対し回帰の n が 848 になった）。
- **群間比較の相手は「選択外（母集団 − 選択中）」。** 母集団全体とは比べない。
  選択に使った列は文章要約・変数重要度から外し、表では「選択に使用」と示す。
- **テーブルの中身を変えたら `db.preaggregator.dropSchema()` を呼ぶ。**
  事前集計テーブルはクエリ文字列のハッシュ名で `CREATE TABLE IF NOT EXISTS`
  されるため、同じ列構成の別データでも古い集計が再利用される。
  ファイル読み込み時と、機械学習の結果を列として書き戻したときに呼んでいる。
- **グラフの種類と選択の操作**（`chartTypes.ts` / `charts.ts`）。
  散布図・ヒストグラム・棒グラフ（件数 / 平均）・平均±誤差棒・折れ線・
  Q-Q プロット・バイオリン図・回帰の残差プロット・相関行列・散布図行列（5列まで）。
  X・Y の列を選ぶと、その型の組み合わせで描ける種類だけを出す
  （数値×数値=散布図［整数の X なら折れ線も］、数値=ヒストグラム、
  カテゴリ=棒（件数）、カテゴリ×数値=誤差棒・棒（平均）、日付×数値=折れ線）。
  カテゴリは X にだけ置く。数値の軸は範囲のドラッグ（intervalX / XY）、
  カテゴリの軸はクリック（toggleX）で、どちらも $brush に書き込む。
  **箱ひげ図は無い**: vgplot に対応するマークが無く、ひげの端は集計関数1つでは
  求まらないため、既存マークの組み合わせでは正しく描けない（ユーザーの指示
  「vgplot に無いマークは無理に自作しない」に従った）。
- **カテゴリのグラフの色付きの層は $brush ではなく $selected で絞る。**
  crossfilter では自分のグラフでクリックした選択が自分に効かず、どれを選んだか
  見えないため。vgplot の highlight は、$brush に合流している絞り込みの条件を
  集計済みの行に当てはめて SQL が失敗し、グラフが描けなかったので使わない。
- **点のグラフ（散布図・残差・散布図行列のマス）の色付きの層も $selected（即時）で絞る。**
  理由はカテゴリのグラフと同じで、自分で囲んだ範囲を自分の点にも色で示すため
  （`charts.ts` の selectedLayer）。dot のときだけで、raster は背景の層が無く枠の外が
  消えてしまうので $brush のまま。自分の描き直しが増えても 15,000 行未満なら
  1フレーム 100ms 未満（`docs/performance.md`「散布図自身の選択の強調」）。
- **誤差棒は errorbarY マークではなく ruleX + dot に SQL 集計式を渡して描く。**
  errorbarY は「平均 ± 倍数 × 標準誤差」しか描けず、標準偏差に切り替えられないため
  （新しいマークは作っていない）。誤差は標準誤差が既定。
- **vgplot に無い図は Observable Plot で描き、連動は Mosaic に任せる**（`statCharts.ts`）。
  集計は makeClient で母集団・選択中それぞれに投げ、グラフ上の操作は vgplot の
  interactor と同じ形の節（clausePoints / clauseInterval）で $brush に書く。
  節の source は interval なら `field`、クリックなら `fields` を持たせる
  （選択のチップ・Esc・カード削除時の解除がそのまま効く）。
  集計が届くたびに図を作り直すので、**ドラッグ中は描き直しを待たせる**
  （InteractionGate。待たせないと、別のグラフで選んだ直後のドラッグが消えた）。
- **vgplot の interactor は、直前のマークの列を選択の対象にする。**
  列を持たないマーク（基準線の ruleY([0]) など）を最後に置くと、範囲選択が
  「NULL BETWEEN …」になって何も選べない（残差プロットで実際に起きた）。
  基準線などは先に描く。
- **階層クラスタリングの行数の上限は 800 行**（`MAX_HCLUST_ROWS`）。超えたら
  hash(rowid) の順で間引き、何件から何件に減らしたかを表示する。根拠は
  `docs/performance.md`「階層クラスタリングの行数の上限」: 3ブラウザ × 3連結法で
  「実行」から図が出るまで2秒以内。1,000 行では Firefox が 2.2〜2.7 秒だった。
  連結法は Ward が初期値、z-score 標準化は初期値オン（オフにすると単位の違いで
  偏る旨を画面に出す）。結果は「階層クラスタ」列として書き戻し、間引いた行と
  範囲外の行は最も近いクラスタの重心に割り当てる。
- **機械学習で書き戻したカテゴリ列は、絞り込みにも加える**（filters.ts の
  setCategoryColumn）。列を使っているグラフだけ作り直し、他は選択肢の更新だけ
  （全部作り直すと無関係なグラフの選択まで消える）。
- **raster マークの0行クラッシュへの対処**（`rasterPatch.ts`）。mosaic-plot 0.31.0 の
  raster は結果が0行だと落ち、直前の画像が残って誤読させる。0行なら空の画像にする。
  ライブラリで直ったら消す。
- **グラフの設定を変えたり削除したりしたら、そのグラフが作った選択だけを解除する。**
  他のグラフの選択は残す（`chartGrid.ts` の dispose）。
- **機械学習の結果は新しいチャートではなく列として書き戻す**（クラスタ・主成分得点）。
  既存の色分け・軸の選択肢に加わり、そのままクロスフィルタが効くようにするため。

## コード規約

- **コメントと UI 文言は日本語**。変数名・関数名は英語。
- TypeScript の型を明示する。`tsconfig.json` の strict を維持し、
  `any` は既存コードにあるもの（Mosaic 側の型定義が薄い箇所）以外では使わない。
- 関数には何をするかだけでなく、**なぜそうしたか**を書く。設計判断が読み取れることを優先する。
- 外部ライブラリを増やすときは、既存の依存で代替できないか先に検討する。
  依存を足す場合は `package.json` と README の構成表を同時に更新する。

## 環境上の注意

- DuckDB-WASM 本体は初回アクセス時に jsDelivr から取得される。
  オフライン環境やCDNブロック環境では動かない。
- `docs/performance.md` の計測はヘッドレス Chromium（Playwright経由）で
  行った。Safari / Firefox での動作・性能は未検証。
- GitHub Pages 配信は `base: '/brushlink/'` 固定。ローカル開発時も
  `npm run dev` の URL が `/brushlink/` 始まりになる。

## 次にやること

優先度順。上から着手する。

1. ~~**ファイル読み込み（CSV / Excel）**~~ — 完了
   （`web/src/upload.ts`。ドロップ領域 + プレビュー表の行クリックで
   ヘッダ行を選ぶ方式。自動推定を初期選択にし、外れても選び直せる。
   SheetJSで両形式を共通処理し、DuckDB-WASMにテーブル登録。
   登録されたテーブルは 'uploaded' という名前で、チャート・フィルタ・
   統計量はすべてこれを対象にする。ファイルが無くても試せるよう
   「サンプルデータで試す」ボタン（`web/src/sample.ts`）も同じ経路を通す）

2. ~~**軸選択とフィルタパネル**~~ — 完了
   （`web/src/filters.ts`。X/Y軸・色分けのセレクタ、数値はデュアル
   レンジスライダー、カテゴリはチップ型チェックボックス、列ごとの
   「欠測を含める」、すべてリセット。フィルタ（$filter）とチャートの
   選択（$brush）は別系統で、$brush は $filter を include する）

3. ~~**統計量と選択件数の表示**~~ — 完了
   （`web/src/stats.ts`。選択中/母集団/全体の件数タイルと入れ子の帯グラフ、
   数値列ごとの n・欠測・平均・SD・最小・中央値・最大と、母集団との
   標準化差（SD単位）。集計は選択中・母集団それぞれ1クエリで DuckDB 側）

4. ~~**単回帰・群間比較**~~ — 完了
   （`regression.ts` / `stats.ts` / `categories.ts` / `inference.ts` / `insights.ts`。
   ライブラリは **jStat** に決めた。p 値に要るのは t 分布・χ² 分布の累積分布関数
   だけで、平均・分散・回帰係数は DuckDB の集計関数で求まる。simple-statistics は
   分布関数を持たず p 値を出せないため不採用。回帰は母集団・選択中の2本、
   群間比較は選択中 vs 選択外の Welch の t 検定＋効果量 d、カテゴリは χ² 検定＋
   Cramér の V。結果の上位を日本語で要約する）

5. ~~**図の書き出し**~~ — 完了
   （`export.ts`。画面のチャート群を位置関係ごと1枚の SVG に合成し、軸・件数・
   選択範囲・絞り込みの注記を付けて SVG / PNG（2倍解像度）で保存）

6. ~~**k-means / PCA / 変数重要度**~~ — 完了
   （`ml.ts` / `ml.worker.ts` / `mlPanel.ts`。ライブラリは使わず自前実装
   （3手法で依存が3つ増える、変数重要度を出せる軽量な JS 実装がない、シード固定で
   再現性を保証したい、のため）。k は 2〜6 からシルエット係数で自動選択。
   変数重要度は選択中/選択外を当てる balanced random forest の Gini 重要度と
   OOB の balanced accuracy。UMAP は未実装）

次の候補:

- ~~ブラウザ駆動の自動テスト（Playwright）~~ — 完了（`web/e2e/`、`npm run test:e2e`）
- ~~1万〜10万行の間の計測と、dot/raster 閾値の見直し~~ — 完了（50,000 → 15,000）
- ~~Safari / Firefox での動作検証~~ — 動作は完了（e2e を WebKit・Firefox でも実行し全件通過、
  画面も目視確認）。**性能は未検証**: 計測に使った long task の API が WebKit・Firefox に
  無いため、閾値 15,000 が両ブラウザでも妥当かは分かっていない。
  実機の Safari（ヘッドレスの WebKit ではなく）でも未確認
- ~~ウィンドウのリサイズへの追従~~ — 完了（作り直さずに Plot の幅だけ変えて描き直すので、
  選択が残る。main.ts の resizePlots）

## やらないこと

- **サーバーを必要とする構成に戻さないこと。** Python/Panel のような
  常時稼働サーバー前提の構成には戻らない。ブラウザ完結を維持する
  （作り直した理由そのものであるため）。
- 認証・マルチユーザー対応（ポートフォリオの範囲外）
- 深層学習ライブラリ（CNN 等）の導入。表形式データと手法が噛み合わず、
  クロスフィルタという軸から外れるため
